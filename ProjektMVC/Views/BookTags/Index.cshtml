@model IEnumerable<ProjektMVC.Models.BookTag>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Book.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tag.value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Book.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.value)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.bookTagID }) |
            @Html.ActionLink("Details", "Details", new { id=item.bookTagID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.bookTagID })
        </td>
    </tr>
}

</table>
